0. Download Homebrew

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"


uise homebrew to Download Xcode developer tools.


# Flutter Installation Test App

This is a basic Flutter app that displays "Hello, Flutter!" to check if Flutter is installed correctly.

## Steps to run the app:

1. Navigate to the project folder:
   ```bash
   cd flutter_app


2. Fetch dependencies:
flutter pub get

3. Run The app
Run the app:


### Step 5: Initialize the Git Repository
Once you've added all the files, initialize a Git repository in the `Flutter Script` directory:

```bash
git init
git add .
git commit -m "Add boilerplate Flutter application"
